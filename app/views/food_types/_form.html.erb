<span class="error_message"><%= flash[:alert] %></span>
<%= form_for @food_type do |f| %>
<%= f.label :name, t('food_type.fields.food_type_name') + ':' %>
<%= f.text_field :name %>

<%= f.submit button_label,id:'submit' %>
<% end %>
