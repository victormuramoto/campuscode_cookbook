<span class="error_message"><%= flash[:alert] %></span>
<%= form_for @kitchen do |f| %>
<%= f.label :name, t('kitchen.fields.kitchen_name') + ':' %>
<%= f.text_field :name %>

<%= f.submit button_label,id:'submit' %>
<% end %>
